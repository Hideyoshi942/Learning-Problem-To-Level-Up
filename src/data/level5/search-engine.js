const m = (slug,order,title,emoji,desc,project,concepts) => ({slug,order,title,emoji,description:desc,project,problems:[{icon:'🚧',title:'Coming Soon',desc:'Nội dung đang được chuẩn bị.'}],concepts,demos:[{id:'cs',label:'🚧 Coming Soon',language:'javascript',code:`console.log('${title}');`}],interactive:null,callouts:[{type:'info',icon:'🚧',title:'Đang phát triển',body:`Demos cho "${title}" đang được chuẩn bị!`}]})
export default m('search-engine',28,'Search Engine','🔎','Crawler, inverted index, PageRank cho search engine từ đầu.','Search Engine.',[
  {name:'Crawler',icon:'🕷️',explain:'Web Crawler (Spider) tự động duyệt và download web pages. Bắt đầu từ seed URLs, extract links, add vào queue, tiếp tục crawl. Cần xử lý: politeness (robots.txt, crawl delay), distributed crawling, deduplication (URL bloom filter), và re-crawl scheduling (fresher pages crawl thường xuyên hơn).',tip:'Respect robots.txt và Crawl-delay. Dùng Bloom Filter để dedup URLs (memory efficient). Distributed crawling với message queue. Chromium headless cho JavaScript-heavy pages.',example:'// Simple crawler:\nconst queue = new Queue(["https://example.com"]);\nconst visited = new BloomFilter(1_000_000, 0.01);\n\nasync function crawl() {\n  while (!queue.isEmpty()) {\n    const url = await queue.dequeue();\n    if (visited.has(url)) continue;\n    visited.add(url);\n\n    try {\n      // Respect robots.txt:\n      if (!await robotsParser.isAllowed(url)) continue;\n\n      const html = await fetch(url).then(r => r.text());\n      const links = extractLinks(html, url);\n\n      // Index page content:\n      await indexPage(url, html);\n\n      // Add new URLs to queue:\n      links.filter(l => !visited.has(l)).forEach(l => queue.enqueue(l));\n    } catch (err) {\n      console.error(`Failed to crawl ${url}:`, err.message);\n    }\n  }\n}'},
  {name:'Inverted Index',icon:'📑',explain:'Inverted Index là core của search engine: ánh xạ từ term → list of documents chứa term đó (posting list), kèm position và frequency. Build process: tokenize → normalize (lowercase, stemming) → remove stop words → build index. Lucene là implementation phổ biến nhất (Elasticsearch dùng Lucene).',tip:'Inverted index được build offline (batch indexing). Real-time indexing (near-real-time trong Elasticsearch) buffer writes và flush mỗi 1s. Segment-based: nhiều segments nhỏ → periodically merge.',example:'// Building Inverted Index:\nconst index = new Map(); // term → [{docId, positions}]\n\nfunction indexDocument(docId, content) {\n  const tokens = tokenize(content);\n  tokens.forEach((token, position) => {\n    if (!index.has(token)) index.set(token, []);\n    const postings = index.get(token);\n    const existing = postings.find(p => p.docId === docId);\n    if (existing) {\n      existing.positions.push(position);\n    } else {\n      postings.push({ docId, positions: [position], tf: 1 });\n      existing && existing.tf++;\n    }\n  });\n}\n\n// Search:\nfunction search(query) {\n  const terms = tokenize(query);\n  const results = terms.map(t => new Set(index.get(t)?.map(p => p.docId)));\n  return intersect(results); // AND query\n}'},
  {name:'TF-IDF',icon:'📊',explain:'TF-IDF scoring trong search engine: TF (Term Frequency) = tần suất term trong document. IDF (Inverse Document Frequency) = log(N/df) penalize common terms. Score cao → document relevant hơn. Elasticsearch dùng BM25 (cải tiến TF-IDF) làm default relevance scoring.',tip:'TF-IDF tốt cho simple text matching nhưng không hiểu semantics. Kết hợp với vector embeddings (semantic search) cho kết quả tốt hơn với natural language queries.',example:'// TF-IDF Scoring:\nfunction tfidf(term, docId, corpus) {\n  const doc = corpus.get(docId);\n  const termCount = doc.terms.filter(t => t === term).length;\n\n  // TF: normalized term frequency\n  const tf = termCount / doc.terms.length;\n\n  // IDF: rarity across corpus\n  const docsWithTerm = [...corpus.values()]\n    .filter(d => d.terms.includes(term)).length;\n  const idf = Math.log(corpus.size / (1 + docsWithTerm));\n\n  return tf * idf;\n}\n\n// Score document for a query:\nfunction scoreDocument(query, docId, corpus) {\n  return tokenize(query)\n    .reduce((score, term) => score + tfidf(term, docId, corpus), 0);\n}'},
  {name:'PageRank',icon:'🏆',explain:'PageRank (Larry Page, Google) đánh giá tầm quan trọng của web page dựa trên số lượng và chất lượng của links trỏ vào. Trang được nhiều trang quan trọng khác link đến → PageRank cao → xuất hiện cao trong search results. Iterative computation: PR(A) = Σ PR(T)/C(T) cho tất cả T link đến A.',tip:'PageRank là nền tảng của Google nhưng ngày nay chỉ là 1 trong 200+ ranking signals. Keyword stuffing, link farms đã làm giảm tầm quan trọng tương đối của nó.',example:'// PageRank computation:\nfunction computePageRank(graph, dampingFactor = 0.85, iterations = 100) {\n  const N = graph.nodes.length;\n  let ranks = {};\n  graph.nodes.forEach(node => ranks[node] = 1 / N);\n\n  for (let i = 0; i < iterations; i++) {\n    const newRanks = {};\n    graph.nodes.forEach(node => {\n      // Sum of ranks from pages linking to this node:\n      const inboundSum = graph.getInbound(node)\n        .reduce((sum, from) =>\n          sum + ranks[from] / graph.getOutboundCount(from), 0);\n      newRanks[node] = (1 - dampingFactor) / N + dampingFactor * inboundSum;\n    });\n    ranks = newRanks;\n  }\n  return ranks;\n}'},
  {name:'BM25',icon:'🎯',explain:'BM25 (Okapi BM25) là ranking function cải tiến TF-IDF, được Elasticsearch và Lucene dùng làm default. Xử lý hai vấn đề của TF-IDF: (1) Term saturation – TF cao không tăng score vô hạn, (2) Document length normalization – docs ngắn hơn có lợi thế. k1=1.2, b=0.75 là default params.',tip:'BM25 vượt TF-IDF đáng kể trong hầu hết benchmark. Tune k1 (0-3) và b (0-1) cho domain cụ thể. k1 thấp = ít saturation. b=0 = không normalize theo length.',example:'// BM25 scoring:\nfunction bm25(term, docId, corpus, k1 = 1.2, b = 0.75) {\n  const doc = corpus.get(docId);\n  const avgDocLength = getAvgDocLength(corpus);\n\n  // Term frequency in document:\n  const tf = doc.terms.filter(t => t === term).length;\n\n  // IDF:\n  const N = corpus.size;\n  const df = [...corpus.values()].filter(d => d.terms.includes(term)).length;\n  const idf = Math.log((N - df + 0.5) / (df + 0.5) + 1);\n\n  // BM25 TF with saturation and length normalization:\n  const normalizedTF = (tf * (k1 + 1)) /\n    (tf + k1 * (1 - b + b * doc.terms.length / avgDocLength));\n\n  return idf * normalizedTF;\n}'},
  {name:'Shard',icon:'🔪',explain:'Search Engine sharding: chia index thành nhiều shards để scale. Elasticsearch: 1 index = N primary shards. Mỗi query được fan-out đến tất cả shards, results được merge và sorted. Shard routing: query đến tất cả shards (scatter-gather). Tăng shards = tăng throughput nhưng tăng overhead khi merge.',tip:'Elasticsearch mặc định 1 shard. Chỉ tăng khi index > 30-50GB. Shard không thể bị split sau khi tạo → plan ahead. Thêm replicas (không phải shards) để tăng read throughput.',example:'// Elasticsearch sharding:\nPUT /products\n{\n  "settings": {\n    "number_of_shards": 5,     // Primary shards\n    "number_of_replicas": 1    // 1 replica/shard → 10 shards total\n  }\n}\n\n// Query execution (scatter-gather):\n// 1. Coordinating node nhận query\n// 2. Fan-out đến tất cả 5 primary shards (parallel)\n// 3. Mỗi shard trả về top-K results\n// 4. Coordinating node merge và sort\n// 5. Fetch full docs cho top results (fetch phase)\n// → 5 shards → 5x throughput vs 1 shard!'},
])
