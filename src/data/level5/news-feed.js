const m = (slug,order,title,emoji,desc,project,concepts) => ({slug,order,title,emoji,description:desc,project,problems:[{icon:'🚧',title:'Coming Soon',desc:'Nội dung đang được chuẩn bị.'}],concepts,demos:[{id:'cs',label:'🚧 Coming Soon',language:'javascript',code:`console.log('${title}');`}],interactive:null,callouts:[{type:'info',icon:'🚧',title:'Đang phát triển',body:`Demos cho "${title}" đang được chuẩn bị!`}]})
export default m('news-feed',23,'News Feed','📰','Fan-out on Write vs Fan-out on Read cho social media feed.','Social Network.',[
  {name:'Fan-out on Write',icon:'✍️',explain:'Fan-out on Write (Push Model): khi user đăng post → ngay lập tức write post đó vào news feed cache/inbox của tất cả followers. Read feed = đọc pre-computed inbox (cực nhanh). Nhưng write rất đắt nếu user có nhiều followers (celebrity problem: 100M followers → 100M writes mỗi post).',tip:'Phù hợp khi users có ít followers (<10K). Twitter dùng Fan-out on Write cho non-celebrities. Read latency rất thấp vì feed đã được pre-compute.',example:'// Fan-out on Write:\nasync function publishPost(userId, post) {\n  // Save post:\n  const postId = await db.savePost(post);\n\n  // Get followers (có thể hàng triệu!):\n  const followerIds = await db.getFollowers(userId);\n\n  // Write to each follower\'s feed cache:\n  const pipeline = redis.pipeline();\n  followerIds.forEach(followerId => {\n    pipeline.lpush(`feed:${followerId}`, postId);\n    pipeline.ltrim(`feed:${followerId}`, 0, 999); // Keep 1000\n  });\n  await pipeline.exec();\n}\n\n// Reading feed is FAST:\nasync function getFeed(userId, page = 0) {\n  const postIds = await redis.lrange(`feed:${userId}`, page*20, page*20+19);\n  return db.getPostsByIds(postIds); // batch load\n}'},
  {name:'Fan-out on Read',icon:'👁️',explain:'Fan-out on Read (Pull Model): khi user đọc feed → query posts của tất cả people they follow, merge và sort theo thời gian. Write cực nhanh (chỉ save 1 post). Read đắt hơn (phải merge nhiều lists). Phù hợp cho users có nhiều followings hoặc celebrities.',tip:'Fan-out on Read cần caching để chịu được traffic. Combine với Fan-out on Write: celebrities dùng on Read, normal users dùng on Write. Twitter dùng hybrid approach này.',example:'// Fan-out on Read:\nasync function getFeed(userId, limit = 20) {\n  // 1. Lấy danh sách following:\n  const followingIds = await db.getFollowing(userId);\n\n  // 2. Query posts của từng người (IN clause):\n  const posts = await db.query(`\n    SELECT * FROM posts\n    WHERE user_id = ANY($1)\n    ORDER BY created_at DESC\n    LIMIT $2\n  `, [followingIds, limit * 3]); // Fetch extra for ranking\n\n  // 3. Merge, rank, paginate:\n  return rankAndFilter(posts, userId).slice(0, limit);\n}\n// ⚠️ Slow nếu following 500 người, mỗi người 100 posts/ngày'},
  {name:'Timeline',icon:'📅',explain:'Timeline là sorted list of posts cho mỗi user feed. Thường sort theo created_at DESC hoặc theo ranking score. Redis Sorted Set là perfect structure: ZADD timeline userId timestamp postId → O(log n) insert, O(log n + k) range query. LinkedIn, Twitter dùng Redis Sorted Set cho timeline.',tip:'Redis Sorted Set ZRANGE với score=timestamp cho time-based timeline. Dùng ZRANGEBYSCORE để paginate theo time range. Max timeline size thường giới hạn 1000 items.',example:'// Redis Sorted Set Timeline:\n// ZADD feed:{userId} {timestamp} {postId}\n\nasync function addToTimeline(userId, postId, timestamp) {\n  await redis.zadd(`feed:${userId}`, timestamp, postId);\n  // Trim to 1000 items:\n  await redis.zremrangebyrank(`feed:${userId}`, 0, -1001);\n}\n\n// Paginate:\nasync function getTimeline(userId, page = 0) {\n  const start = page * 20;\n  const end = start + 19;\n  // Newest first (reverse order):\n  const postIds = await redis.zrevrange(`feed:${userId}`, start, end);\n  return db.batchGetPosts(postIds);\n}'},
  {name:'Ranking',icon:'⭐',explain:'Ranking algorithm quyết định thứ tự hiển thị posts. Không phải chronological – EdgeRank (Facebook cũ), GraphRank, ML-based. Factors: recency, engagement (likes/comments/shares), relationship strength (thường xuyên interact), content type preference. Modern feeds dùng ML models.',tip:'Simple ranking: score = likes×2 + comments×3 + shares×5 - age_hours×0.1. Complex: trained ML model với user-specific features. Start simple, iterate.',example:'// Simple ranking function:\nfunction rankPost(post, viewerUserId) {\n  const ageHours = (Date.now() - post.createdAt) / 3_600_000;\n\n  // Engagement score:\n  const engagement = post.likes * 2\n    + post.comments * 3\n    + post.shares * 5;\n\n  // Relationship boost:\n  const relationBoost = post.authorId in viewerCloseFreinds ? 2 : 1;\n\n  // Decay over time:\n  const timeDecay = Math.exp(-0.1 * ageHours);\n\n  return engagement * relationBoost * timeDecay;\n}\n\n// Sort feed by score:\nposts.sort((a, b) => rankPost(b, userId) - rankPost(a, userId));'},
  {name:'Pagination',icon:'📄',explain:'News Feed pagination cần Cursor-based (Keyset) thay vì OFFSET vì: (1) Feed thay đổi liên tục (mới insert, delete), (2) Infinite scroll UX. Cursor = timestamp của post cuối cùng. Request tiếp theo: posts WHERE created_at < cursor. Fan-out on Write: cursor trong Redis Sorted Set (ZREVRANGEBYSCORE).',tip:'Encode cursor như opaque token (Base64). Đừng expose raw timestamp – người dùng có thể manipulate. Cho phép pull-to-refresh = query posts mới hơn cursor đầu tiên.',example:'// Cursor pagination cho news feed:\nasync function getFeedPage(userId, cursor = null) {\n  let postIds;\n\n  if (!cursor) {\n    // First page:\n    postIds = await redis.zrevrange(`feed:${userId}`, 0, 19);\n  } else {\n    // Decode cursor:\n    const lastScore = JSON.parse(atob(cursor)).score;\n    postIds = await redis.zrevrangebyscore(\n      `feed:${userId}`,\n      lastScore - 1, // Exclusive\n      "-inf",\n      "LIMIT", 0, 20\n    );\n  }\n\n  const posts = await db.batchGetPosts(postIds);\n  const nextCursor = posts.length > 0\n    ? btoa(JSON.stringify({ score: posts.at(-1).timestamp }))\n    : null;\n\n  return { posts, nextCursor };\n}'},
])
