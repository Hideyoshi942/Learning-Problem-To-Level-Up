const m = (slug,order,title,emoji,desc,project,concepts) => ({slug,order,title,emoji,description:desc,project,problems:[{icon:'🚧',title:'Coming Soon',desc:'Nội dung đang được chuẩn bị.'}],concepts,demos:[{id:'cs',label:'🚧 Coming Soon',language:'javascript',code:`console.log('${title}');`}],interactive:null,callouts:[{type:'info',icon:'🚧',title:'Đang phát triển',body:`Demos cho "${title}" đang được chuẩn bị!`}]})
export default m('video-streaming',24,'Video Streaming','🎬','CDN, chunk upload, adaptive bitrate cho video streaming platform.','Video Platform.',[
  {name:'CDN',icon:'🌐',explain:'CDN (Content Delivery Network) là mạng lưới edge servers phân tán toàn cầu, cache và serve content từ vị trí gần user nhất. Thay vì stream video từ origin server ở US → user ở VN phải travel 15,000km, CDN cache video tại Singapore → latency giảm 80%. CloudFront, Cloudflare, Fastly là CDN phổ biến.',tip:'Video files không thể cache ở CDN layer thông thường (quá lớn). Thay vào đó: CDN cache các video segments nhỏ (2-10s mỗi segment). Pre-warm popular content lên edge nodes.',example:'// CDN URL pattern:\n// Origin: storage.example.com/videos/abc123/720p/segment001.ts\n// CDN:    cdn.example.com/videos/abc123/720p/segment001.ts\n\n// CloudFront config:\n{\n  "Origins": [{\n    "DomainName": "storage.example.com",\n    "Id": "VideoOrigin"\n  }],\n  "DefaultCacheBehavior": {\n    "ViewerProtocolPolicy": "redirect-to-https",\n    "CachePolicyId": "video-cache-policy",\n    // Cache video segments 7 ngày:\n    "TTL": 604800\n  }\n}\n\n// Signed URL cho private videos:\nconst signedUrl = cloudfront.getSignedUrl({\n  url: "https://cdn.example.com/video/abc123",\n  expires: Date.now() / 1000 + 3600 // 1 giờ\n});'},
  {name:'HLS',icon:'📺',explain:'HLS (HTTP Live Streaming) là Apple\'s adaptive streaming protocol. Video được chia thành segments nhỏ (2-10s, thường .ts format). Master playlist (.m3u8) chứa danh sách các quality variants. Media playlist chứa URLs của segments. Player tự động chọn quality phù hợp bandwidth.',tip:'HLS có latency cao hơn DASH (~20-30s). Low-Latency HLS (LL-HLS) giảm xuống 2-3s. Hỗ trợ rộng rãi trên iOS, macOS, Smart TVs. Default cho Apple platforms.',example:'// HLS Master Playlist (master.m3u8):\n#EXTM3U\n#EXT-X-STREAM-INF:BANDWIDTH=800000,RESOLUTION=640x360\n360p/playlist.m3u8\n#EXT-X-STREAM-INF:BANDWIDTH=1400000,RESOLUTION=1280x720\n720p/playlist.m3u8\n#EXT-X-STREAM-INF:BANDWIDTH=2800000,RESOLUTION=1920x1080\n1080p/playlist.m3u8\n\n// Media Playlist (720p/playlist.m3u8):\n#EXTM3U\n#EXT-X-TARGETDURATION:6\n#EXTINF:6.0,\nsegment001.ts\n#EXTINF:6.0,\nsegment002.ts\n#EXTINF:5.8,\nsegment003.ts'},
  {name:'DASH',icon:'📊',explain:'DASH (Dynamic Adaptive Streaming over HTTP) là open-source adaptive streaming tiêu chuẩn ISO. Tương tự HLS nhưng dùng MPD file (XML) thay vì m3u8. Hỗ trợ video segments dạng MP4 (fMP4). Linh hoạt hơn HLS cho DRM, multiple audio tracks, subtitle. YouTube, Netflix dùng DASH.',tip:'DASH không được hỗ trợ native trên iOS Safari → cần dash.js player. Kết hợp DASH + EME (Encrypted Media Extensions) cho DRM. fMP4 segments compatible hơn TS.',example:'// DASH MPD (manifest):\n<?xml version="1.0"?>\n<MPD xmlns="urn:mpeg:dash:schema:mpd:2011" type="dynamic">\n  <Period>\n    <AdaptationSet mimeType="video/mp4">\n      <!-- 360p -->\n      <Representation id="1" bandwidth="800000"\n        width="640" height="360">\n        <SegmentTemplate media="360p_$Number$.m4s"\n          initialization="360p_init.mp4" duration="6"/>\n      </Representation>\n      <!-- 720p -->\n      <Representation id="2" bandwidth="1400000"\n        width="1280" height="720">\n        <SegmentTemplate media="720p_$Number$.m4s"\n          initialization="720p_init.mp4" duration="6"/>\n      </Representation>\n    </AdaptationSet>\n  </Period>\n</MPD>'},
  {name:'Chunk Upload',icon:'📦',explain:'Chunk Upload (Multipart Upload) chia file lớn thành nhiều chunks để upload. Nếu connection bị đứt → chỉ cần resume từ chunk chưa upload. AWS S3 Multipart Upload: tối thiểu 5MB/part. Tus protocol là open standard cho resumable uploads. YouTube dùng Google\'s resumable upload API.',tip:'Upload chunks song song để tăng tốc. Minimum chunk size 5MB (S3 requirement). Lưu upload state ở client (localStorage) để resume sau khi browser refresh.',example:'// Tus resumable upload:\nimport * as tus from "tus-js-client";\n\nconst upload = new tus.Upload(file, {\n  endpoint: "https://api.example.com/upload/",\n  retryDelays: [0, 3000, 5000, 10000], // Auto retry\n  chunkSize: 5 * 1024 * 1024, // 5MB chunks\n  metadata: { filename: file.name, filetype: file.type },\n  onError: (error) => console.error(error),\n  onProgress: (bytesUploaded, bytesTotal) => {\n    const percentage = (bytesUploaded / bytesTotal * 100).toFixed(2);\n    console.log(`${percentage}%`);\n  },\n  onSuccess: () => console.log("Upload complete!")\n});\nupload.start(); // Resume nếu đã có previous upload'},
  {name:'Transcoding',icon:'⚙️',explain:'Transcoding là quá trình chuyển đổi video từ format/codec này sang khác và tạo nhiều quality variants (360p, 720p, 1080p, 4K). CPU/GPU intensive. Workflow: upload raw video → message queue → transcoding workers (FFmpeg) → output segments → store S3 → invalidate CDN cache.',tip:'Dùng hardware acceleration: NVIDIA NVENC, Intel QuickSync, AWS MediaConvert. Transcoding cost ~1s video = 10-30s processing. Queue system (SQS, Kafka) để scale workers theo demand.',example:'// FFmpeg transcoding command:\n$ ffmpeg -i input.mp4 \\\\\n  -vf "scale=1280:720" \\\\\n  -c:v libx264 -preset fast -crf 23 \\\\\n  -c:a aac -b:a 128k \\\\\n  -hls_time 6 \\\\\n  -hls_playlist_type vod \\\\\n  -hls_segment_filename "720p_%03d.ts" \\\\\n  720p/playlist.m3u8\n\n// AWS MediaConvert job:\nawait mediaconvert.createJob({\n  Role: "arn:aws:iam::...",\n  Settings: {\n    Inputs: [{ FileInput: "s3://raw/video.mp4" }],\n    OutputGroups: [/* HLS output config */]\n  }\n});'},
  {name:'ABR',icon:'📶',explain:'ABR (Adaptive Bitrate) là kỹ thuật tự động chọn quality phù hợp với bandwidth của user. Player đo throughput liên tục, nếu bandwidth giảm → switch xuống quality thấp hơn (tránh buffering), nếu tăng → switch lên quality cao hơn. Các algorithms: BOLA, MPC, pensieve (ML-based).',tip:'Buffer target thường 15-30s. Switch quality dựa trên: buffer level, estimated bandwidth, segment download time. Không switch quá thường xuyên (gây flicker). Segment boundary là điểm switch.',example:'// Simple ABR algorithm:\nclass SimpleABR {\n  estimateBandwidth(downloadTime, segmentSize) {\n    // bits per second:\n    return (segmentSize * 8) / downloadTime;\n  }\n\n  selectQuality(bandwidth, buffer) {\n    // Low buffer → aggressive downgrade:\n    if (buffer < 5) return "360p";\n\n    // Select best quality that fits bandwidth:\n    const qualities = [\n      { name: "1080p", bitrate: 2_800_000 },\n      { name: "720p",  bitrate: 1_400_000 },\n      { name: "480p",  bitrate: 1_000_000 },\n      { name: "360p",  bitrate:   800_000 },\n    ];\n\n    // Use 80% of bandwidth (safety margin):\n    const safeBandwidth = bandwidth * 0.8;\n    return qualities.find(q => q.bitrate <= safeBandwidth)?.name ?? "360p";\n  }\n}'},
])
