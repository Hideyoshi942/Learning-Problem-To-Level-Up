const m = (slug,order,title,emoji,desc,project,concepts) => ({slug,order,title,emoji,description:desc,project,problems:[{icon:'🚧',title:'Coming Soon',desc:'Nội dung đang được chuẩn bị.'}],concepts,demos:[{id:'cs',label:'🚧 Coming Soon',language:'javascript',code:`console.log('${title}');`}],interactive:null,callouts:[{type:'info',icon:'🚧',title:'Đang phát triển',body:`Demos cho "${title}" đang được chuẩn bị!`}]})
export default m('distributed-fs',29,'Distributed File Storage','📁','Replication, consistency, erasure coding cho distributed storage.','File Storage.',[
  {name:'Replication Factor',icon:'📑',explain:'Replication Factor (RF) là số copies của mỗi data block trong distributed file system. HDFS default RF=3: data stored on 3 DataNodes. Nếu 1 node fail → 2 còn lại. RF=3 tolerate tối đa 2 node failures. Trade-off: RF cao → storage cost cao, RF thấp → ít durable hơn.',tip:'RF=3 là tiêu chuẩn cho production. Rack-aware replication: đặt replicas trên different racks để survive rack-level failures. AWS S3 replicate across 3 AZs (11 nines durability).',example:'// HDFS Replication:\n// File: video.mp4 (1GB) với RF=3\n// HDFS chia thành blocks (128MB mặc định):\n// Block 1 → DataNode 1 (Rack A), DataNode 3 (Rack B), DataNode 5 (Rack C)\n// Block 2 → DataNode 2 (Rack A), DataNode 4 (Rack B), DataNode 6 (Rack C)\n// ...\n\n// Thay đổi RF:\nhadoop fs -setrep -w 5 /user/data/important.parquet\n// → 5 copies cho critical data!\n\n// Check replication status:\nhdfs fsck /user/data -files -blocks -locations\n// → Shows block locations và missing replicas'},
  {name:'Erasure Coding',icon:'⚙️',explain:'Erasure Coding là alternative cho replication: thay vì lưu N copies, chia data thành k data blocks + m parity blocks (k+m blocks total). Có thể recover data từ bất kỳ k blocks nào trong k+m. Reed-Solomon (8+4) = 12 blocks nhưng chỉ cần 8 để recover → 50% overhead thay vì 200% với RF=3.',tip:'Erasure Coding tiết kiệm storage đáng kể (50% vs 200% overhead). Nhưng: recovery phức tạp hơn (đọc k nodes, compute), higher read latency, higher CPU usage. Phù hợp cho cold/warm storage.',example:'// Reed-Solomon (6, 3): 6 data + 3 parity = 9 chunks\n// Có thể recover khi mất bất kỳ 3 chunks nào\n\n// Encoding:\nconst encoder = new ReedSolomon(6, 3);\nconst dataChunks = splitFile(file, 6); // 6 equal parts\nconst [d1,d2,d3,d4,d5,d6] = dataChunks;\nconst [p1,p2,p3] = encoder.encode(dataChunks);\n// Lưu 9 chunks trên 9 nodes khác nhau\n\n// Recovery khi mất chunks 2 và 5:\nconst available = [d1, null, d3, d4, null, d6, p1, p2, p3];\nconst recovered = encoder.decode(available);\n// → Original file recovered! ✅\n// Storage overhead: 9/6 = 1.5x (vs 3x với RF=3)'},
  {name:'Consistency',icon:'🔄',explain:'Distributed storage consistency models: Strong Consistency (mọi read thấy latest write – Amazon S3 default từ 2020), Eventual Consistency (reads eventually consistent – DynamoDB default), Read-after-Write Consistency (read ngay sau write thấy data mới). CAP theorem: không thể có cả 3: Consistency, Availability, Partition tolerance.',tip:'Amazon S3 có Strong Consistency miễn phí từ Dec 2020. HDFS: strong consistency (single writer). GCS: strong consistency. Object storage thường strong, NoSQL thường tunable.',example:'// Consistency levels trong Cassandra:\n// Write CL=QUORUM: ghi vào majority nodes\n// Read CL=QUORUM: đọc từ majority nodes\n// → Strong consistency (W + R > RF)\n\nconst session = cassandra.connect();\nawait session.execute(\n  "INSERT INTO files (id, data) VALUES (?, ?)",\n  [fileId, data],\n  { consistency: cassandra.types.consistencies.quorum } // Write to majority\n);\n\nconst result = await session.execute(\n  "SELECT data FROM files WHERE id=?",\n  [fileId],\n  { consistency: cassandra.types.consistencies.quorum } // Read from majority\n);\n// W=2 + R=2 > RF=3 → Always reads latest write! ✅'},
  {name:'HDFS',icon:'🐘',explain:'HDFS (Hadoop Distributed File System) là distributed file system của Apache Hadoop. Architecture: 1 NameNode (metadata: file names, blocks, locations) + nhiều DataNodes (actual data blocks). Write-once-read-many pattern. Optimized cho large files, sequential reads. Block size mặc định 128MB.',tip:'HDFS NameNode là single point of failure → cần HA NameNode (Active + Standby). Không phù hợp cho small files (nhiều small files overload NameNode metadata). Dùng Sequence Files hoặc ORC để bundle small files.',example:'// HDFS Architecture:\n// Client → NameNode: "Write file video.mp4"\n// NameNode: "Split into blocks, store on DN1, DN2, DN3"\n// Client → DN1: write block 1\n// DN1 → DN2 → DN3: pipeline replication\n\n// HDFS Commands:\nhadoop fs -put localfile.mp4 /user/hadoop/videos/  # Upload\nhadoop fs -get /user/hadoop/videos/video.mp4 .     # Download\nhadoop fs -ls /user/hadoop/videos/                  # List\nhadoop fs -du -h /user/hadoop/                      # Disk usage\n\n// Java API:\nFileSystem fs = FileSystem.get(conf);\nFSDataOutputStream out = fs.create(new Path("/video.mp4"));\nFileUtils.copyFile(localFile, out);'},
  {name:'Object Storage',icon:'☁️',explain:'Object Storage (S3, GCS, Azure Blob) lưu data dưới dạng objects trong flat namespace (không phải hierarchy). Mỗi object có: key (path), data, metadata, version. Hầu hết scalable, strongly consistent (S3 2020+), với 11 nines durability. REST API. Tối ưu cho large unstructured data.',tip:'Object Storage rẻ hơn Block Storage 5-10x. Dùng cho: media files, backups, data lake, static website hosting. Nhược điểm: không mount như filesystem, không support random writes (immutable objects).',example:'// AWS S3 operations:\nimport { S3Client, PutObjectCommand, GetObjectCommand } from "@aws-sdk/client-s3";\n\nconst s3 = new S3Client({ region: "ap-southeast-1" });\n\n// Upload:\nawait s3.send(new PutObjectCommand({\n  Bucket: "my-bucket",\n  Key: "videos/2024/movie.mp4",  // Flat key, not real path!\n  Body: fileBuffer,\n  ContentType: "video/mp4",\n  Metadata: { "uploaded-by": "user-123" }\n}));\n\n// Presigned URL (allow direct upload from browser):\nconst url = await getSignedUrl(s3, new PutObjectCommand({\n  Bucket: "my-bucket", Key: "uploads/video.mp4"\n}), { expiresIn: 3600 }); // 1 hour\n// Client uploads directly to S3 (không qua server)'},
])
